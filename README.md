# jobijoba-test

Suivre les commandes suivantes pour lancer le projet :

```
git clone https://github.com/sebastien-jj/jobijoba-test.git
cd jobijoba-test
docker compose up -d
docker exec -it app_apache sh
composer install
```
